version: 2
project_name: prodigi-pp-cli
changelog:
  disable: true
builds:
  - id: prodigi-pp-cli
    main: ./cmd/prodigi-pp-cli
    binary: prodigi-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X prodigi-pp-cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: prodigi-pp-mcp
    main: ./cmd/prodigi-pp-mcp
    binary: prodigi-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
brews:
  - name: prodigi-pp-cli
    repository:
      owner: dexploarer
      name: homebrew-tap
    homepage: "https://github.com/dexploarer/prodigi-pp-cli"
    description: "CLI for prodigi"
    install: |
      bin.install "prodigi-pp-cli"
      bin.install "prodigi-pp-mcp"
